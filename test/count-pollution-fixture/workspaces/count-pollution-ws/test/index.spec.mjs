import { test } from "node:test";
import assert from "node:assert/strict";

test("main test 1", () => assert.equal(1, 1));
test("main test 2", () => assert.equal(1, 1));
test("main test 3", () => assert.equal(1, 1));
test("main test 4", () => assert.equal(1, 1));
test("main test 5", () => assert.equal(1, 1));
